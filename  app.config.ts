export default defineAppConfig({
    ui: {
        colors: {
            primary: '#e12c33',      // brand red
            primaryHover: '#c02027',
            primaryLight: '#f16368',
            primaryBg: '#fee2e2',
            primaryBorder: '#fca5a5',
            secondary: '#1f2937',    // dark gray (neutral contrast)
            tertiary: '#6b7280',     // medium gray
            accent: '#fbbf24',       // warm yellow accent
            background: '#f9fafb',   // light gray background (like bg-gray-100)
            foreground: '#ffffff',   // white (for cards, surfaces)
            border: '#d1d5db',       // subtle gray border
            text: '#111827',         // near-black for readability
            error: '#dc2626',        // red for errors
            success: '#16a34a',      // green for success
            warning: '#f59e0b',      // amber for warnings
            info: '#2563eb',         // blue for info
            muted: '#9ca3af',        // muted gray text
            light: '#f3f4f6',        // very light gray
        }
    }
}
)
